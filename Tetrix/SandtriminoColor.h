#pragma once
#include<easy2d/easy2d.h>

using namespace easy2d;

enum SandtriminoColor {
    BLUE = Color::Blue,
    RED = Color::Red,
    GREEN = Color::Green,
    BROWN = Color::Gold,
    NONE = Color::Black
};